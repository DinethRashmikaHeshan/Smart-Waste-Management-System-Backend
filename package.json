{
  "name": "smart-waste-management-system-backend",
  "version": "1.0.0",
  "description": "Y3S1-CSSE",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",   // Use node instead of nodemon for production
    "build": "webpack --config webpack.config.js" // If you use Webpack; remove this if not needed
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.20.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.1",
    "mongoose": "^8.7.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.7"
  }
}
